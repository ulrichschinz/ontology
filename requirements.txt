# Ontology skill v2.0.0 dependencies

# Required — graph database backend
kuzu>=0.9.0

# Required — schema files
pyyaml>=6.0

# Optional — semantic search layer (install both or neither)
# lancedb>=0.20.0
# sentence-transformers>=3.0.0
